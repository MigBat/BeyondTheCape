military_hegemon = {
	allow = {
		OR = {
			# Great Power
			AND = {
				is_great_power = yes
				is_potential_hegemon = no
				if = {
					limit = {
						OR = {
							has_country_flag = halve_hegemony_requirements
							is_emperor_of_china = yes
						}
					}
					army_size = 250 
				}
				else = { army_size = 500 } 
				NOT = { any_other_great_power = { army_size = root } }
			}
			# Discovery Potential
			AND = {
				current_age = age_of_discovery
				is_discovery_mil_hegemon = yes
				army_size = 50
				most_army_discovery_mil_hegemon = yes
			}
			# Refomation Potential
			AND = {
				current_age = age_of_reformation
				is_reformation_mil_hegemon = yes
				army_size = 100
				most_army_reformation_mil_hegemon = yes
			}
			# Absolutism Potential
			AND = {
				current_age = age_of_absolutism
				is_absolutism_mil_hegemon = yes
				army_size = 150
				most_army_absolutism_mil_hegemon = yes
			}
			# Revolutions Potential
			AND = {
				current_age = age_of_revolutions
				is_revolutions_mil_hegemon = yes
				army_size = 200
				most_army_revolutions_mil_hegemon = yes
			}
		}
		NOT = { has_country_modifier = lost_hegemony }
	}
	
	base = {
		country_military_power = 1
		war_exhaustion = -0.1
		province_warscore_cost = -0.1
	}
	
	# At max 100% tickup.
	scale = {
		movement_speed = 0.1
		land_attrition = -0.2
		global_unrest = -3
	}
	
	max = {
		siege_ability = 0.2
	}
	
}